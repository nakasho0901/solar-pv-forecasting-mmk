# -*- coding: utf-8 -*-
# train.py が参照するデータセット設定（dataset_name + "_conf"）
custom_conf = {
    "data_root": "dataset_PV",                # -d で上書き可能
    "save_root": "save",                      # -s で上書き可能
    # 追加の自由メモ（実装側で必要なら参照）
    "notes": "Tsukuba PV + Weather splits under dataset_PV/",
}
